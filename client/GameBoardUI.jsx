const GameBoardUI = (props) => {
  // Create canvasEl and ctx
  // https://stackoverflow.com/questions/2745432/best-way-to-detect-that-html5-canvas-is-not-supported
  // if (!(canvasEl.getContext && canvasEl.getContext('2d') && canvasEl.toDataURL && canvasEl.toDataURL()))
  return (
    <>{'('}Game board UI will go here{')'}</>
  )
}

module.exports = GameBoardUI;
